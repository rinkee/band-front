# 주문 추가 기능 구현 계획

## 📋 요구사항 정리

### 기능 개요
- **버튼 위치**: "주문 수정" 버튼 옆에 "주문 추가" 버튼 배치
- **상품 범위**: 같은 밴드(band)의 상품만 표시
- **UI 형태**: 모달 팝업
- **기본값**:
  - 상태: 주문완료
  - 수량: 1
  - 가격: 상품 기본 가격 자동 적용
- **고객 정보**: 기존 고객 정보 유지 (customer_id, customer_name 등)

### 사용자 플로우
1. 주문 추가 버튼 클릭
2. 모달 팝업에서 같은 밴드의 상품 리스트 표시
3. 상품 선택
4. 수량 입력 (기본값: 1)
5. 가격 자동 계산 표시
6. 확인 버튼 클릭
7. 선택한 고객과 선택한 상품으로 새 주문 생성
8. DB에 저장 후 UI 자동 갱신

---

## 🏗️ 구현 단계

### Phase 1: 주문 추가 모달 컴포넌트 생성

**파일**: `app/components/AddOrderModal.jsx` (신규 생성)

#### 컴포넌트 구조
```jsx
function AddOrderModal({
  isOpen,
  onClose,
  customer,
  bandKey,
  userId,
  onOrderAdded
}) {
  // Props:
  // - isOpen: 모달 열림/닫힘 상태
  // - onClose: 모달 닫기 함수
  // - customer: 기존 고객 정보 객체
  // - bandKey: 현재 주문의 band_key (상품 필터링용)
  // - userId: 현재 사용자 ID
  // - onOrderAdded: 주문 추가 완료 후 콜백
}
```

#### 주요 기능
1. **상품 목록 조회**
   - API: `/api/products?userId=${userId}&bandKey=${bandKey}`
   - 같은 band_key의 상품만 필터링
   - useEffect로 모달이 열릴 때 상품 목록 로드

2. **상품 선택 드롭다운**
   - 상품명과 기본 가격 표시
   - 선택 시 가격 자동 설정

3. **수량 입력 필드**
   - type="number"
   - min="1"
   - 기본값: 1
   - 수량 변경 시 total_amount 자동 계산

4. **가격 정보 표시**
   - 상품 단가 (price_per_unit)
   - 수량
   - 총 금액 (total_amount) = 단가 × 수량
   - 읽기 전용 (자동 계산)

5. **버튼**
   - 확인: 주문 생성 API 호출
   - 취소: 모달 닫기

6. **로딩 상태**
   - 상품 목록 로딩 중 표시
   - 주문 생성 중 버튼 비활성화

---

### Phase 2: OrdersTable.jsx 수정

**파일**: `app/components/OrdersTable.jsx`

#### 수정 사항

1. **주문 추가 버튼 추가** (308번 줄 "주문 수정" 버튼 근처)
```jsx
<button
  className="inline-flex items-center justify-center px-3 py-1.5 text-sm text-blue-600 bg-blue-100 hover:text-blue-800 hover:bg-blue-200 rounded-md transition-colors"
  title="주문 추가"
  onClick={() => handleAddOrderStart(order)}
>
  주문 추가
</button>
```

2. **상태 관리 추가**
```jsx
const [addOrderModal, setAddOrderModal] = useState({
  isOpen: false,
  customer: null,
  bandKey: null
});
```

3. **핸들러 함수 구현**
```jsx
// 주문 추가 모달 열기
const handleAddOrderStart = (order) => {
  setAddOrderModal({
    isOpen: true,
    customer: {
      customer_id: order.customer_id,
      customer_name: order.customer_name,
      customer_band_id: order.customer_band_id,
      customer_profile: order.customer_profile,
      customer_phone: order.customer_phone
    },
    bandKey: order.band_key
  });
};

// 주문 추가 완료 후 처리
const handleOrderAdded = async (newOrder) => {
  setAddOrderModal({ isOpen: false, customer: null, bandKey: null });
  // SWR mutate로 주문 목록 갱신
  await mutate('/api/orders');
  // 성공 메시지 표시 (옵션)
};
```

4. **AddOrderModal 컴포넌트 import 및 렌더링**
```jsx
import AddOrderModal from './AddOrderModal';

// JSX 내부
{addOrderModal.isOpen && (
  <AddOrderModal
    isOpen={addOrderModal.isOpen}
    onClose={() => setAddOrderModal({ isOpen: false, customer: null, bandKey: null })}
    customer={addOrderModal.customer}
    bandKey={addOrderModal.bandKey}
    userId={userId}
    onOrderAdded={handleOrderAdded}
  />
)}
```

---

### Phase 3: 주문 생성 API 구현

**파일**: `app/api/orders/create/route.js` (신규 생성)

#### API 엔드포인트 설계

**Method**: POST

**Request Body**:
```json
{
  "userId": "user_id",
  "customerId": "customer_id",
  "productId": "product_id",
  "quantity": 1,
  "bandKey": "band_key",
  "bandNumber": "band_number"
}
```

**Response** (성공):
```json
{
  "success": true,
  "order": {
    "order_id": "generated_order_id",
    "customer_name": "고객명",
    "product_name": "상품명",
    "quantity": 1,
    "total_amount": "23500",
    "status": "주문완료",
    "ordered_at": "2025-01-19T10:30:00Z"
  }
}
```

**Response** (실패):
```json
{
  "success": false,
  "error": "에러 메시지"
}
```

#### API 로직

1. **요청 검증**
   - userId, customerId, productId, quantity 필수 확인
   - quantity > 0 확인

2. **데이터 조회**
   - Customer 정보 조회 (customers 테이블)
   - Product 정보 조회 (products 테이블)
   - Product의 band_key가 요청의 bandKey와 일치하는지 확인

3. **Order 객체 생성**
```javascript
const orderId = `order_${userId}_${bandKey}_${productId}_${Date.now()}`;

const orderData = {
  order_id: orderId,
  user_id: userId,
  customer_id: customerId,
  customer_name: customerInfo.customer_name,
  customer_band_id: customerInfo.band_user_no,
  customer_profile: customerInfo.profile_image,
  customer_phone: customerInfo.phone,
  product_id: productId,
  product_name: productInfo.product_name,
  band_key: bandKey,
  band_number: bandNumber || productInfo.band_number,
  post_key: productInfo.post_key,
  quantity: quantity,
  price: productInfo.price,
  price_per_unit: productInfo.price,
  total_amount: (parseFloat(productInfo.price) * quantity).toString(),
  status: '주문완료',
  sub_status: '미수령',
  processing_method: 'manual_add',
  ordered_at: new Date().toISOString(),
  created_at: new Date().toISOString(),
  updated_at: new Date().toISOString()
};
```

4. **DB 저장**
```javascript
const { data, error } = await supabase
  .from('orders')
  .insert([orderData])
  .select()
  .single();
```

5. **에러 처리**
   - 고객 없음: "고객 정보를 찾을 수 없습니다"
   - 상품 없음: "상품 정보를 찾을 수 없습니다"
   - Band 불일치: "해당 밴드의 상품이 아닙니다"
   - DB 에러: "주문 생성 중 오류가 발생했습니다"

---

### Phase 4: 상품 조회 API (기존 활용 또는 신규)

#### 옵션 1: 기존 API 수정
**파일**: 기존 상품 조회 API 찾아서 bandKey 필터 파라미터 추가

#### 옵션 2: 신규 API 생성
**파일**: `app/api/products/by-band/route.js` (신규)

**Method**: GET

**Query Parameters**:
- userId: 사용자 ID
- bandKey: 밴드 키

**Response**:
```json
{
  "success": true,
  "products": [
    {
      "product_id": "prod_xxx",
      "product_name": "상품명",
      "price": "10000",
      "band_key": "band_key",
      "stock": 100
    }
  ]
}
```

**로직**:
```javascript
const { data, error } = await supabase
  .from('products')
  .select('*')
  .eq('user_id', userId)
  .eq('band_key', bandKey)
  .order('created_at', { ascending: false });
```

---

## 📊 데이터베이스 스키마

### orders 테이블 주요 컬럼 (참고)

```sql
- order_id (PK): 주문 고유 ID
- user_id: 사용자 ID
- customer_id (FK): 고객 ID
- product_id (FK): 상품 ID
- band_key: 밴드 키
- band_number: 밴드 번호
- post_key: 게시물 키
- quantity: 수량
- price: 단가
- price_per_unit: 개당 단가
- total_amount: 총 금액
- status: 주문 상태 (주문완료, 픽업완료 등)
- sub_status: 세부 상태 (미수령, 수령완료 등)
- processing_method: 처리 방법 (manual_add)
- ordered_at: 주문 시간
- created_at: 생성 시간
- updated_at: 수정 시간
- customer_name: 고객명
- customer_band_id: 밴드 사용자 번호
- customer_profile: 프로필 이미지
- customer_phone: 전화번호
- product_name: 상품명
```

---

## ✅ 체크리스트

### Phase 1: 모달 컴포넌트
- [ ] AddOrderModal.jsx 파일 생성
- [ ] 상품 목록 조회 로직 구현
- [ ] 상품 선택 드롭다운 UI
- [ ] 수량 입력 필드 UI
- [ ] 가격 자동 계산 로직
- [ ] 주문 생성 API 호출
- [ ] 로딩 상태 처리
- [ ] 에러 처리 및 표시

### Phase 2: OrdersTable 수정
- [ ] 주문 추가 버튼 추가
- [ ] 모달 상태 관리
- [ ] handleAddOrderStart 함수
- [ ] handleOrderAdded 함수
- [ ] AddOrderModal import
- [ ] 모달 렌더링
- [ ] SWR mutate 연동

### Phase 3: API 구현
- [ ] /api/orders/create/route.js 생성
- [ ] 요청 검증 로직
- [ ] 고객 정보 조회
- [ ] 상품 정보 조회
- [ ] band_key 일치 확인
- [ ] order_id 생성 로직
- [ ] orderData 객체 구성
- [ ] DB insert
- [ ] 에러 처리
- [ ] 응답 반환

### Phase 4: 상품 조회 API
- [ ] bandKey 필터 기능 확인/추가
- [ ] API 응답 형식 확인
- [ ] 모달에서 API 연동 테스트

### 테스트
- [ ] 개발 DB에서 주문 추가 테스트
- [ ] 같은 밴드의 상품만 표시되는지 확인
- [ ] 가격 자동 계산 확인
- [ ] 기본값 (상태, 수량) 확인
- [ ] 고객 정보 정확히 유지되는지 확인
- [ ] UI 갱신 (SWR) 작동 확인
- [ ] 에러 케이스 테스트 (상품 없음, 권한 없음 등)

---

## 🚨 주의사항

### 보안
- ✅ userId 기반 권한 체크 필수
- ✅ SQL Injection 방지 (Supabase 사용으로 자동 방지)
- ✅ 입력값 검증 (quantity, productId 등)

### 데이터 무결성
- ✅ customer_id, product_id FK 제약 조건 확인
- ✅ band_key 일치 여부 검증
- ✅ 트랜잭션 불필요 (단일 insert)

### 프로덕션 환경
- ⛔ 프로덕션 DB 직접 수정 금지
- ⛔ 프로덕션 Edge Function 배포 금지
- ✅ 개발 DB에서만 테스트
- ✅ task.md에 작업 내용 기록

### 사용자 경험
- ✅ 로딩 상태 표시
- ✅ 에러 메시지 명확히 표시
- ✅ 주문 추가 후 즉시 UI 반영
- ✅ 모달 닫기 시 상태 초기화

---

## 📁 파일 구조

```
app/
├── components/
│   ├── AddOrderModal.jsx          ✨ 신규 생성
│   └── OrdersTable.jsx             🔧 수정
├── api/
│   ├── orders/
│   │   └── create/
│   │       └── route.js            ✨ 신규 생성
│   └── products/
│       └── by-band/
│           └── route.js            ✨ 신규 생성 (옵션)
└── hooks/
    └── useOrdersClient.js          (기존 활용)
```

---

## 🔄 데이터 흐름

```
사용자 클릭 (주문 추가)
    ↓
OrdersTable.jsx
    → handleAddOrderStart()
    → setAddOrderModal({ isOpen: true, customer, bandKey })
    ↓
AddOrderModal.jsx 렌더링
    → useEffect: 상품 목록 조회 (GET /api/products?bandKey=xxx)
    → 사용자: 상품 선택 + 수량 입력
    → 확인 버튼 클릭
    ↓
주문 생성 API 호출 (POST /api/orders/create)
    → 요청 검증
    → Customer 정보 조회
    → Product 정보 조회
    → band_key 일치 확인
    → orderData 객체 생성
    → Supabase insert
    → 응답 반환
    ↓
AddOrderModal.jsx
    → onOrderAdded() 콜백 실행
    → 모달 닫기
    ↓
OrdersTable.jsx
    → SWR mutate() 호출
    → 주문 목록 자동 갱신
    → 새 주문이 테이블에 표시됨
```

---

## 🎯 예상 결과

1. **주문 수정 버튼 옆에 "주문 추가" 버튼 표시**
2. **클릭 시 모달 팝업**
3. **같은 밴드의 상품 목록만 드롭다운에 표시**
4. **상품 선택 시 가격 자동 설정**
5. **수량 입력 시 총 금액 자동 계산**
6. **확인 클릭 시 orders 테이블에 새 레코드 생성**
7. **기존 고객 정보 그대로 유지**
8. **주문 목록에 새 주문 즉시 반영**

---

**작성일**: 2025-01-19
**작성자**: Claude
**프로젝트**: Band 주문 관리 시스템
